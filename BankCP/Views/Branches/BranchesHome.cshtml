@model IEnumerable<BusinessObjects.Models.Branch>

@{
    ViewBag.Title = "BranchesHome";
}

@if (TempData["msg"] != null)
{
    @Html.Raw(TempData["msg"])
    TempData["msg"] = null;
}
<h2>BranchesHome</h2>
<div class="row">
    <div class="col-md-2">
        @Html.ActionLink(BankCP.Resources.LangText.createBranch, "AddBranch")
    </div>
    <div class="col-md-offset-8 col-2">
        @Html.ActionLink(BankCP.Resources.LangText.services, "ServiceHome", "Services")
    </div>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName(BankCP.Resources.LangText.enName)
        </th>
        <th>
            @Html.DisplayName(BankCP.Resources.LangText.arName)
        </th>
        <th>
            @Html.DisplayName(BankCP.Resources.LangText.active)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        using (Html.BeginForm("DeleteBranch", "Branches", new { branchId = item.id }))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.enName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.arName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.active)
                </td>
                <td>
                    @Html.ActionLink(BankCP.Resources.LangText.edit, "EditBranch", new { branchId = item.id }) |
                    @Html.ActionLink(BankCP.Resources.LangText.counters, "CounterHome", "Counters", new { branchId = item.id },null) |
                    <input type="submit" value="@BankCP.Resources.LangText.services" class="buttonLink" onclick="return confirm('Are you sure you want to delete @item.enName');" />
                </td>
            </tr>
        }
    }

</table>
