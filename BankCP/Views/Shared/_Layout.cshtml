@using GlobalResource.Resources

<!DOCTYPE html>
<html lang="@LangText.language" dir="@LangText.direction">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @if (LangText.language == "en")
    {
        @Styles.Render("~/Content/css")
    }
    else
    {
        @Styles.Render("~/Content/rtl/css")
    }
    @Styles.Render("~/Content/css-main")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/sweetalert")
</head>
<body>
    @if (ViewBag.errorMsg != null)
    {
        <script>Swal.fire({ title: 'Error!', text: '@ViewBag.errorMsg', icon: 'error', confirmButtonText: 'ok' })</script>
        ViewBag.errorMsg = null;
    }

    @if (ViewBag.loginMsg != null)
    {
        <script>Swal.fire({ title: 'Error!', text: '@ViewBag.loginMsg', icon: 'error', confirmButtonText: 'ok' })</script>
        ViewBag.loginMsg = null;
    }
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink(LangText.applicationName, "Home", "Branches", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @if (LangText.languageText == "English")
                    {
                        <li class="revert-float">@Html.ActionLink(LangText.languageText, "Change", "Lang", new { lang = "En" }, new { style = "font-size: 17px;" })</li>
                    }
                    else
                    {
                        <li class="revert-float">@Html.ActionLink(LangText.languageText, "Change", "Lang", new { lang = "Ar" }, new { style = "font-size: 18px;" })</li>
                    }
                    @using (Html.BeginForm("logout", "Login", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        if (Session["UserObj"] != null)
                        {
                            <li class="revert-float">
                                <input type="submit" value="@LangText.signOut" class="input-custom"/>
                            </li>
                        }
                    }
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
        </footer>
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
